kurigram==2.2.21
py-tgcalls==2.3.3
TgCrypto==1.2.5
python-dotenv==1.2.2
# Pinned to a specific yt-dlp master commit (NOT the latest stable). The
# stable releases after 2026.6.9 (incl. 2026.07.04) carry the Instagram
# "empty media response" regression (yt-dlp/yt-dlp#17074): anonymous
# reel/post extraction fails. This commit contains the fix and keeps
# YouTube working. Verified: IG + YouTube both download. Bump to a normal
# ">=X.Y.Z" pin once a stable release ships the fix.
yt-dlp @ git+https://github.com/yt-dlp/yt-dlp.git@498e51f5da47539f3b4cc52ff85be5f33e7e9d2f
aiohttp==3.14.1
# InnerTube-based YouTube search — works from datacenter IPs where
# yt-dlp's ytsearch returns empty (fixes text /play "song not found").
py-yt-search>=0.6.2
Pillow==12.2.0
cryptography>=44.0.0
# Durable persistence for all bot state (served chats/users, stats, per-chat
# settings, sudo/blacklist/gban, log-channel) when REDIS_URL is set — survives
# Railway redeploys. Falls back to local JSON when unset/unreachable.
redis==5.2.1
